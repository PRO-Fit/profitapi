# Note: temp directory to deal with the calendar Oauth 2.0 redirect logic
#       It stores the temp data for the user for particular email account and deletes it once stored in DB

database = {}
session = {}